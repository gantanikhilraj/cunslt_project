import 'package:flutter/material.dart';

class CustomColorsNew {
  static const Color textAccent = Color(0xFF01D5BF);
  static const Color textBlue = Color(0xFF1E88E5);
}

const textFieldDecorationEmail = InputDecoration(
  hintText: 'Enter Email',
  contentPadding: EdgeInsets.symmetric(vertical: 10.0, horizontal: 20.0),
  border: OutlineInputBorder(
    borderRadius: BorderRadius.all(Radius.circular(32.0)),
  ),
);
const textFieldDecorationPassword = InputDecoration(
  hintText: 'Enter Password',
  contentPadding: EdgeInsets.symmetric(vertical: 10.0, horizontal: 20.0),
  border: OutlineInputBorder(
    borderRadius: BorderRadius.all(Radius.circular(32.0)),
  ),
);
